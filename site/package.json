{
  "name": "hymarkx-site",
  "private": true,
  "type": "module",
  "description": "hymarkx.afsah.xyz — written in HyMarkX, built with the published hymarkx package.",
  "scripts": {
    "build": "node build.mjs",
    "dev": "hmx dev . --trust document"
  },
  "dependencies": {
    "hymarkx": "0.0.6"
  }
}
